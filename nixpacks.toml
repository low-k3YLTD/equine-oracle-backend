[phases.setup]
nixPkgs = ["python311", "gcc", "libffi", "openssl"]  # python311 instead of python3

[phases.install]
cmds = ["pip install --upgrade pip setuptools wheel", "pip install -r requirements.txt"]

[start]
cmd = "npm start"  # or your actual start command (python main.py, uvicorn, etc.)
